# users-api-system

To run this project:

1) `npm i`;
2) `npm run dev`;


Run 

`node db-init`
 
 to initialize database;
 
 1) Database initialization was wrong, that's why it was cleared on each reload;